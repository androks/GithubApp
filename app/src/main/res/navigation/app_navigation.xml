<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/album_nav_graph"
    app:startDestination="@id/repositoryFragment">
    <fragment
        android:id="@+id/repositoryFragment"
        android:name="com.androks.githubapp.presentation.list.RepositoryListFragment"
        android:label="RepositoryListFragment">
        <action
            android:id="@+id/start"
            app:destination="@id/repositoryFragment"/>
    </fragment>
</navigation>